import { LitElement } from "lit";
import { Constructor } from "./Constructor";
import { AttachInternalsMixin } from "./AttachInternals";
/**
 * Mixin to augment an element with behavior that suppresses initial animations.
 * @template T The type of the base class.
 * @param {T} base The base class.
 * @returns {T} A constructor that implements initial animation suppression.
 */
export declare function SuppressInitialAnimation<T extends Constructor<LitElement & AttachInternalsMixin>>(base: T): T;
//# sourceMappingURL=SuppressInitialAnimation.d.ts.map